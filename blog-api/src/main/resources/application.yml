server:
  port: 8888

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/yu-blog?useUnicode=true&characterEncoding=UTF-8&serverTimeZone=UTC
    username: root
    password: e23s5
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: localhost
    port: 6379
  main:
    allow-circular-references: true
  servlet:
    multipart:
      # 單個檔案的最大值
      max-file-size: 2MB
      # 上傳檔案總共的最大值
      max-request-size: 20MB

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      table-prefix: yu_

file:
  path: /Users/cheng/IdeaProjects/blog-yu/blog-api/src/main/resources/file/


